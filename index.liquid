<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
</head>
<body>
  {% if clients.size > 0 %}
    <b>Current Clients</b>
    <br />
    {% for client in clients %}
      <p>{{ client[0] }}</p>
      <br />
    {% endfor %}
  {% else %}
    No clients connected
  {% endif %}
  <br />
  Skin <select onchange="window.location = './skin?s=' + this.options[this.selectedIndex].value">
  {% assign skins = "Por Defecto,Gris,Neon,Chino,Carreras" | split: ',' %}
  {% for s in (0..skins.size) %}
    <option value="{{ s }}"{% if skin == s %} selected{% endif %}>{{ skins[s] }}</option>
  {% endfor %}
  </select>
  <br />
  Ganador <select onchange="window.location = './winner?w=' + this.options[this.selectedIndex].value">
  <option value="00"{% if winner == 37 %} selected{% endif %}>00</option>
  {% for s in (0..36) %}
    <option value="{{ s }}"{% if winner == s %} selected{% endif %}>{{ s }}</option>
  {% endfor %}
  </select>
  <br />
  <input type="button" value="Sortear" onclick="window.location = './raffle'" />
</body>
</html>